lifetab_basic <- function(studbook, span = 5) {
  x <- 0:max_age
  k <- length(x)
  lx <- c(1, cumprod(px)[-k])
}

survshp <- function(px, k) {
  lx <- c(1, cumprod(px)[-k])
  return(lx)
}


x <- "age class"
"number alive per age class"
qx <- list("age-specific mortality rate" = ("the probability of dying during a specific age class (e.g., between the ages of 0 1 or 5 6 years of age).  Calculated, for each sex, as (the number of deaths per age class)/(number of individuals at risk for Qx)."))
